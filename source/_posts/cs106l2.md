---
title: "CS106L 学习笔记（二）：C++ 类型系统与结构体"
date: 2024-11-24
categories: ["编程"]
tags: ["C++", "CS106L", "类型系统", "结构体", "编译原理"]
---

# CS106L 第二讲：类型系统与结构体

> 斯坦福 CS106L 2025 Fall 第二讲整理

---

## 🔄 编译 vs 解释

### 解释型语言（如Python）
```python
print("Hello World")
print("Welcome to ")
for ch in "CS106L":
    print(ch)
```
- **逐行翻译执行**
- 错误在**运行时**被发现
- 所有操作在**运行时间**完成

### 编译型语言（如C++）
```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello World" << endl;
    cout << "Welcome to " << endl;
    for (char ch : "CS106L") {
        cout << ch << endl;
    }
    return 0;
}
```
- **整个程序先编译**成可执行文件
- 错误在**编译时**就被捕获
- 分为**编译时间**和**运行时间**

---

## 🆚 静态类型 vs 动态类型

### 动态类型（Python）
```python
a = 3
b = "test"

def foo(c):
    d = 106
    d = "hello world!"  # 可以改变类型！
```
- 解释器在运行时根据变量值分配类型
- 类型可以随意改变

### 静态类型（C++）
```cpp
int a = 3;
string b = "test";

void foo(string c) {
    int d = 106;
    d = "hello world!";  // 编译错误！
}
```
- **每个变量必须声明类型**
- **一旦声明，类型不能改变**

### 静态类型的优势
- **更高效**
- **易于理解和推理**
- **更好的错误检查**

---

## 🏗️ C++ 类型系统

### 基本数据类型
- `int`, `float`, `double`, `char`, `bool`, `void`

### 派生数据类型
- 数组、指针、引用、函数

### 用户定义类型
- `class`, `struct`, `union`, `typedef`, `using`

---

## 📦 结构体（Struct）

### 定义结构体
```cpp
struct Student {
    string name;      // 字段（field）
    string sunet;     // 每个字段有名称和类型
    int idNumber;
};
```

### 使用结构体
```cpp
// 传统初始化
Student id;
id.name = "THE Stanford Tree";
id.sunet = "theTREE";
id.idNumber = 0000002;

// 统一初始化
Student tree = {"THE Stanford Tree", "theTREE", 0000002};
Student lelandjr {"Leland Stanford Jr", "thejunior", 5430282};
```

### 结构体作为返回值
```cpp
Student issueNewID() {
    Student id = {"THE Stanford Tree", "theTREE", 0000002};
    return id;
}
```

---

## 🔗 std::pair 模板

### 基本使用
```cpp
#include <utility>

std::pair<string, int> dozen {"Eggs", 12};
cout << dozen.first;   // "Eggs"
cout << dozen.second;  // 12
```

### 嵌套使用
```cpp
// 解二次方程的返回类型
std::pair<bool, std::pair<double, double>> solveQuadratic(double a, double b, double c);
```

### 模板定义
```cpp
template <typename T1, typename T2>
struct pair {
    T1 first;
    T2 second;
};
```

---

## 📚 标准库 std

### 使用标准库
```cpp
#include <string>    // std::string
#include <utility>   // std::pair  
#include <iostream>  // std::cout, std::endl
```

### 命名空间最佳实践
```cpp
// 推荐：使用 std:: 前缀
std::cout << "Hello" << std::endl;

// 不推荐：using namespace std;
// 可能引起命名冲突
```

---

## 🛠️ 实用特性

### using 类型别名
```cpp
// 长类型名很繁琐
std::pair<bool, std::pair<double, double>> solveQuadratic(double a, double b, double c);

// 使用类型别名
using Zeros = std::pair<double, double>;
using Solution = std::pair<bool, Zeros>;
Solution solveQuadratic(double a, double b, double c);
```

### auto 类型推断
```cpp
// 传统写法
std::pair<bool, std::pair<double, double>> result = solveQuadratic(a, b, c);

// 使用 auto
auto result = solveQuadratic(a, b, c);  // 编译器自动推断类型

// auto 仍然是静态类型！
auto i = 1;        // int 类型
i = "hello!";      // 编译错误！
```

---

## 💡 核心概念总结

- **C++是编译型、静态类型语言**
- **结构体**将命名变量捆绑成新类型
- **std::pair**是通用的双字段结构体
- **#include**引入标准库功能
- **using**创建类型别名，**auto**推断变量类型

---

## 🎯 实战练习

尝试实现一个解二次方程的函数：
```cpp
#include <utility>
#include <cmath>

using Zeros = std::pair<double, double>;
using Solution = std::pair<bool, Zeros>;

Solution solveQuadratic(double a, double b, double c) {
    double discriminant = b*b - 4*a*c;
    
    if (discriminant < 0) {
        return {false, {0.0, 0.0}};  // 无解
    }
    
    double sqrt_d = std::sqrt(discriminant);
    double x1 = (-b + sqrt_d) / (2*a);
    double x2 = (-b - sqrt_d) / (2*a);
    
    return {true, {x1, x2}};  // 返回解
}
```

> 下一讲我们将探讨初始化与引用，深入了解C++的内存管理机制！
```